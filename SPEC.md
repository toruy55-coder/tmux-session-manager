# tmux セッション管理ツール 仕様書

## 1. 背景・目的

MacからVaioU（VAIO U、192.168.0.215）にSSH接続し、tmux上で複数の作業を並行して立ち上げている。Macを持ち出す・スリープする等でSSH接続が切れても、tmuxセッション自体はVaioU側で生き続ける。しかし時間が経つと「どのセッションが何の作業だったか」を忘れてしまい、再接続時にどれにアタッチすべきか分からなくなる。

本ツールは、
- 今どのセッションが存在し、接続中か切断中かを一覧表示する
- 各セッションに「何の作業か」のメモを付けて後から思い出せるようにする
- 一覧から選んでそのままアタッチできる

ことを目的とする。

## 2. 用語

- **セッション**: tmuxのセッション（`tmux new -s <名前>`で作成する単位）
- **アタッチ中**: そのセッションに現在クライアント（SSH経由の端末）が接続されている状態
- **デタッチ中**: セッションは存在するが、現在どの端末からも接続されていない状態
- **メモ**: セッション名に紐づけて保存する自由記述のテキスト（「何の作業か」を記録する）

## 3. データ設計

### 3.1 メモの保存先

- パス: `~/sync/logs/tmux-sessions_notes.json`（Synology Drive同期フォルダ配下。Mac側にも自動同期される）
- ログとして残す位置づけのため、ホーム直下の隠しファイルではなく`~/sync/logs/`に置く。将来Obsidian連携で書き込む際もこのファイルを参照する想定（Mac側から書き込む運用も可）
- 形式: JSON。セッション名をキーとしたオブジェクト

```json
{
  "kyoto-kacho": {
    "memo": "ITパスポート問題難易度判定のバッチ処理を回している",
    "updated_at": "2026-08-26T10:30:00+09:00"
  },
  "blastmail-dev": {
    "memo": "メルマガ下書きスキルのバグ修正",
    "updated_at": "2026-08-20T15:00:00+09:00"
  }
}
```

- tmuxそのものにはメモを保存する仕組みがないため、別ファイルで名前をキーに管理する（要件どおり）。
- **セッション名がキー**なので、同名でセッションを作り直すとメモは引き継がれる（意図的な挙動）。
- tmuxセッションが`kill-session`で消えても、メモファイルのエントリは自動削除しない（後述4.5で手動削除コマンドを用意）。誤って有用なメモを消さないための保守的な設計。

### 3.2 tmux側から取得する情報

`tmux list-sessions`および`tmux list-clients`から取得できる情報を利用する（追加のtmux設定は不要）:
- セッション名
- アタッチ中かどうか（クライアント数 > 0か）
- 作成日時
- 最終アクティビティ日時
- 現在の作業ディレクトリ（アクティブなペインのカレントディレクトリ）

## 4. 機能一覧

### 4.1 一覧表示（メインコマンド、引数なし）

セッションを一覧表示する。表示項目:

| 番号 | 状態 | セッション名 | 作業ディレクトリ | 最終アクティビティ | メモ |
|---|---|---|---|---|---|
| 1 | ●接続中 | kyoto-kacho | ~/github/clients/kyoto-kacho | 2分前 | ITパスポート難易度判定バッチ |
| 2 | ○切断中 | blastmail-dev | ~/github/blastmail-newsletter | 3日前 | メルマガ下書きスキルのバグ修正 |

- 「状態」は接続中/切断中を色または記号で視覚的に区別する
- メモが未設定の場合は空欄または `(メモなし)` と表示
- 番号は後述のアタッチ・メモ編集コマンドで使う一時的な連番（実行のたびに振り直し）

### 4.2 アタッチ

- `番号`または`セッション名`を指定してアタッチする
- 一覧表示から続けて番号を選ぶ対話形式、または `<コマンド> attach <番号|名前>` の直接指定の両方に対応する

### 4.3 新規セッション作成

- `<コマンド> new <セッション名> [メモ]` でtmuxセッションを作成し、同時にメモを保存する
- メモを省略した場合は空メモで作成、または対話的に入力を促す
- セッション名が未指定の場合は対話的に入力を促す（作業ディレクトリ名を初期候補として提示すると使いやすい）

### 4.4 メモの追加・編集

- `<コマンド> memo <番号|名前> "<メモ内容>"` で既存セッションのメモを更新する
- 既存tmuxセッションに対して、後からメモを追加/上書きできる（作成時にメモを付け忘れた場合の救済）

### 4.5 メモの削除・掃除

- `<コマンド> rm <番号|名前>` でメモのみ削除（tmuxセッションはkillしない）
- `<コマンド> gc` で「tmux上に存在しないセッション名」のメモを一括削除する（不要になったメモの掃除用、実行前に削除対象一覧を表示して確認を取る）

## 5. コマンド体系（案）

コマンド名: `tm`（`~/bin/tm`等、PATHの通った場所に配置。既存の`tm`コマンドと衝突しないか事前確認する）

```
tm                          # 一覧表示 → 番号入力でアタッチ（対話モード）
tm list                     # 一覧表示のみ
tm attach <番号|名前>        # 指定セッションにアタッチ
tm new <名前> [メモ]         # 新規セッション作成＋メモ登録
tm memo <番号|名前> "<メモ>"  # メモ更新
tm rm <番号|名前>            # メモ削除（セッションは残す）
tm gc                       # 存在しないセッションのメモを一括削除（確認あり）
```

## 6. 実装方針

- 言語: Python 3（標準ライブラリのみで完結させ、追加インストール不要にする。`subprocess`で`tmux`コマンドを呼び出し、`json`でメモファイルを読み書き）
- 配置: `~/github/tmux-session-manager/`
  - `tm.py`（本体）
  - `README.md`（使い方）
- 実行方法: `~/bin/tm` からシンボリックリンク、またはラッパーで `python3 ~/github/tmux-session-manager/tm.py "$@"` を呼ぶ
- 対象環境: VaioU上での利用のみを想定（Mac側では使わない）

## 6.5 Webダッシュボード（web.py、2026-08-27追加）

CLIとは別に、ブラウザからマウス操作で状態確認・メモ編集ができるダッシュボードを追加した。
「何をやってたか分かりやすく」「マウスで修正・接続できるように」という要望を受けたもの。

### 設計方針

- **ブラウザ内にターミナルは作らない。** 実際のアタッチはSSH+tmuxコマンドをボタンで
  クリップボードにコピーし、手元のターミナルに貼り付けて実行する方式にとどめる。
  ブラウザ内で本物のターミナルを再現する（xterm.js等）実装コストを避けるための判断。
- **手元のアプリ・ブラウザが落ちてもセッションは残る。** これはtmuxサーバーがVaioU側で
  独立して動く構造そのものによって保証されており、Webダッシュボード側で何かを実装する
  必要はない。
- CLI版（`tm.py`）とロジックを共有するため、共通処理を`tm_core.py`に切り出した。
- 依存を増やさないため、Flask等は使わずPython標準ライブラリの`http.server`のみで実装する。

### 機能

- セッション一覧をカード表示（接続中/切断中・作業ディレクトリ・最終操作時刻・メモ）
- 各カードのメモをその場で編集して保存（`POST /memo`）
- 「接続コマンドをコピー」ボタンで `ssh <user>@<host> -t 'tmux attach -t <名前>'` をクリップボードにコピー
- 「使われていないメモ」セクションで、対応するtmuxセッションが無いメモを個別に削除できる（`POST /delete_orphan`。CLIの`tm gc`のWeb版・一括確認ではなく1件ずつ）

### 公開範囲・常駐

- `kadai-kanri`と同じ方針で、**Tailscaleアドレス（`100.74.222.40`）にのみバインド**し、
  社内LAN等tailnet外からは届かないようにする（HTTPS未対応のため）
- ポート: `8765`
- systemdユーザーサービス（`deploy/tm-web.service` → `~/.config/systemd/user/tm-web.service`）
  として常駐。VaioU再起動後も自動復帰する

## 6.6 再起動対策：セッションの永続化と復元（2026-08-30追加）

### 背景

VaioU再起動でtmuxサーバーごとセッションが全消滅する事象が発生した。tmuxセッションは
メモリ上のプロセスのため、`reboot`で`/tmp/tmux-<uid>/`のソケットごと消える。
調査の結果、tmの実装は`-L`/`-S`/`TMUX_TMPDIR`を一切使わない素の`tmux`呼び出しのみで、
ソケット不一致は原因ではなかった。また、セッション定義（名前・作業ディレクトリ・
初期コマンド）を永続化する仕組みがそもそも存在しなかったことが根本原因だった。

### tmux-resurrect/continuum ではなく自前実装を選んだ理由

| 観点 | tmux-resurrect/continuum | 採用：自前実装 |
|---|---|---|
| 依存 | TPM（プラグインマネージャ）導入が必要 | 追加インストール不要（標準ライブラリのみの方針を維持） |
| 復元対象 | ペイン内の実行中プロセスまで復元しようとする（ヒューリスティック、完全ではない） | 名前・作業ディレクトリ・初期コマンドのみ。「プロセスの中身は戻らない」仕様と一致 |
| 設定 | `.tmux.conf`に分散 | `~/.config/tm/sessions.toml`で完結し、`tm_core.py`のメモ管理と同じ設計に載る |

### データ設計

- パス: `~/.config/tm/sessions.toml`
- 形式: TOML。`[[session]]`の配列。各要素は`name`（セッション名）・`dir`（作業ディレクトリ、
  `~`始まりで保存し復元時に展開）・`cmd`（起動直後に流す初期コマンド、空文字なら実行しない）
- 読み込みは標準ライブラリの`tomllib`（Python 3.11+）。書き込みはTOML書き込みライブラリが
  標準に無いため、シンプルな構造専用の手書きシリアライザで対応

### 機能

- `tm save`: 現在動いているtmuxセッションの名前・作業ディレクトリから定義を作り、
  既存の`sessions.toml`に書かれていた`cmd`を名前が一致するものだけ引き継いでから上書き保存する
- `tm restore`: 定義ファイルを読み、**tmux上に存在しないセッションだけ**を
  `tmux new-session -d -s <name> -c <dir>`で作る（**冪等**。既存セッションには触れない）。
  `cmd`が設定されていれば`tmux send-keys`で流す
- 作業ディレクトリは`tm new`（手動作成）と異なり、`tm restore`は既存プロセスへの
  再接続ではなく新規シェル作成のため、**`-c`で明示的に作業ディレクトリを渡す**
  （渡さないとsystemdサービスの実行ディレクトリで開いてしまう）。動作確認済み

### 起動時の自動復元

- `deploy/tm-restore.service`（`Type=oneshot`）を追加。`ExecStart=tm.py restore`を
  起動時（`WantedBy=default.target`）に1回実行する
- SSHしていない間もsystemd --userとtmuxサーバーを生かしておくため`loginctl enable-linger`が
  必要（このマシンは`kadai-kanri`・`tm-web`のために既に有効化済み）

### セッションの共有・奪取

- `tm attach <対象>`: 従来通り`tmux attach-session -t <name>`（`-d`なし）。複数クライアントが
  同時に同じセッションへ接続でき、これが「共有」にあたる（追加実装不要、元々の動作のまま）
- `tm attach <対象> --takeover`: `tmux attach-session -d -t <name>`。既存クライアントを
  切り離して奪取する

## 7. 非対象範囲（今回は作らないもの）

- ブラウザ内での本物のターミナル接続（xterm.js等での実装。SSHコマンドのコピーで代替）
- セッションの自動命名・自動メモ生成（AIによる要約等）
- 複数マシン間でのメモ同期（VaioU単体のローカルファイルのみ。Mac側からの読み書きは今後の課題）
- tmux未インストール環境への対応
- Webダッシュボードからの新規セッション作成（CLIの`tm new`のみ対応）
- `tm restore`によるペイン内プロセスの復元（vimの編集内容・実行中コマンドの状態等は戻らない）

## 8. 未確定事項（実装前に確認したい点）

- コマンド名`tm`が既存のエイリアス・コマンドと衝突しないか
- 対話モードでの番号選択時のキー操作（数字入力+Enterでよいか、矢印キー選択が欲しいか）
- 一覧のソート順（最終アクティビティ順・作成順・セッション名順のどれを既定にするか）
